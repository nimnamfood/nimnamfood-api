# Implementation Plans

Generated by the improve skill on 2026-07-06 (audit at commit `ad32e0f`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Log event-captor failures; tolerate events without captors | P1 | S | — | TODO |
| 002 | Fix view_plan_search projection (plans invisible in GET /plans) | P1 | S | — (001 recommended first) | TODO |
| 003 | Run real Flyway migrations in tests, retire schema.sql | P1 | M | — | TODO |
| 004 | HTTP-layer test baseline for all resources + exception handler | P2 | M | — | TODO |
| 005 | Require a bearer token on all API endpoints | P1 | M | 004 | TODO |
| 006 | Load storage key from file path; keep it out of the jar | P2 | S | — | TODO |
| 007 | Validate uploads by WebP magic bytes, not client header | P2 | S | — | TODO |
| 008 | Order blob operations after the recipe DB save | P3 | M | — (001 recommended first) | TODO |
| 009 | Repo hygiene: dead Main.java, CLAUDE.md, unit-test profile, formatter | P3 | S | — | TODO |
| 010 | Spike: shopping list generated from a plan (design + prototype) | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 requires 004: the auth filter changes every HTTP endpoint's behavior;
  the `@WebMvcTest` suite from 004 is the safety net and gets adapted in 005.
- 001 before 002/008 (soft): once captor failures are logged, any regression
  in the projection/blob work is visible instead of silent.
- 003 interacts with 002: plan 002 adds migration V14. Whichever lands second
  should confirm the migration runs in tests (003 makes that automatic).
- 002 and 003 touch disjoint files despite both mentioning migrations — safe
  in parallel.

## Deployment coordination (operator, not executor)

- 005: set `API_TOKEN` in production and update the frontend to send
  `Authorization: Bearer <token>` BEFORE deploying.
- 006: provision the key file on the production host and set
  `STORAGE_KEY_PATH` BEFORE deploying; rotate the dev service-account key
  afterwards (old jars contain it).

## Findings considered and rejected

- **SQL injection in `JdbcTagQueryTranslator`**: vetted and rejected — the
  concatenated `jsonbTagsColumn` is a hardcoded constructor argument
  (`"tag_ids"`), all user values go through named parameters. Safe as written.
- **Timezone inconsistency (V13 `AT TIME ZONE 'Europe/Berlin'` vs projections
  writing UTC)**: rejected — `timestamptz` stores absolute instants; the
  Berlin clause only interpreted pre-existing naive timestamps once, at
  migration time. Ordering/comparison of old and new rows is correct.
- **Cascading view updates on ingredient/recipe change (perf)**: not worth
  doing — single-user dataset; the JSONB-scan updates are measured in
  milliseconds at this scale. Revisit only with real load evidence.
- **Projection class duplication (`On*Created`/`On*Changed` pairs)**: deferred
  — real but low-value; refactor after the HTTP test baseline (004) exists.
- **No event store / replay for read models**: rejected as disproportionate —
  plan 001 adds the missing observability; eventual consistency is accepted.
- **`FailedToUploadIllustrationException` leaks IOException message (500)**:
  not separately planned — plan 004 pins current exception behavior and flags
  discrepancies; add a handler mapping when touching that file next.
- **Committed `.idea/dataSources`, `src/.DS_Store`**: local-only noise, not
  git-tracked; nothing to do.
- **Direction: plan editing (swap/regenerate a meal, delete plans)**: grounded
  (PUT exists for recipes/ingredients but plans are generate-only; deleting a
  plan would today leave stale view rows — see plan 002's maintenance note),
  but not selected this round. Revisit after the shopping-list spike (010).
