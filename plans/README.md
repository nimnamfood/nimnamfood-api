# Implementation Plans

Generated by the improve skill on 2026-07-07, at commit `ad32e0f`, from a review
of the plan (meal-planning) implementation. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Repo facts every executor needs: Java 21, Spring Boot 3.5.11, Maven wrapper
(`./mvnw`), Postgres + Flyway migrations, tests via Testcontainers (**Docker
must be running** for `./mvnw test`). Commit messages are written in French,
lowercase, imperative (see `git log --oneline`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fill `view_plan_search` when a plan is created | P1 | S | — | TODO |
| 002  | Enforce default and max page size on `GET /plans` | P1 | S | — | TODO |
| 003  | Make projection failures observable; `@Synced` plan projections | P1 | M | 001 (soft) | TODO |
| 004  | Add missing DB constraints and indexes | P2 | S | 001 (migration numbering) | TODO |
| 005  | Move `RecipeTagFilterRequirement` into the domain | P2 | S | — | TODO |
| 006  | `RecipeCandidatePicker` port + memoized candidate queries | P2 | M | 005 | TODO |
| 007  | Join shared recipe summary at read time; delete sync-back projection | P3 | L | 001, 004 (numbering); 003 (soft) | TODO |
| 008  | Run the command bus on the IO executor | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **Migration numbering is the main coupling**: 001 takes `V14`, 004 takes `V15`, 007 takes `V16`. If plans run out of order, each plan says to take the next free number and report the deviation.
- 006 requires 005: the port's signature uses `nimnamfood.model.recipe.RecipeTagFilterRequirement`, which 005 moves out of the infrastructure package.
- 003 is best after 001 so that `@Synced PlanCreated` covers both plan projections (`view_plans` and `view_plan_search`) from day one; it works without it.
- 007 is best after 003 so that failures of the (simplified) `OnPlanCreatedFillSummary` are loud; it works without it.
- 002, 005, 006, 008 are independent of everything except as noted.
- 006 and 007 both assume the current `GeneratePlanCommandHandler` / plan-projection code; if executed, re-run each plan's drift check rather than assuming.

## Findings considered and rejected

- **GIN index on `view_plans.meals`** (to speed up the recipe-change sync-back scan): rejected — plan 007 deletes the scanning projection entirely; indexing it first would be wasted work.
- **Outbox table for durable event delivery**: rejected for now — meaningful infrastructure for a single-user-scale app; plan 003's synchronous plan projections + error logging cover the practical risk. Reconsider if events start driving anything business-critical.
- **Dropping `view_plans` and reading the `plans`/`meals` write tables in `GetPlanHandler`**: deferred — it would delete another projection but crosses the project's "queries read views only" convention; recorded as a maintainer decision in plan 007's notes.
- **Package-by-bounded-context restructure (`nimnamfood.planning.*`)**: rejected as a big-bang move — plans 005/006 fix the actual dependency-direction violations; the package move stays cheap to do later once dependencies point the right way.
- **Clock injection into `Plan`/`Plan.Factory`** (testability of `Instant.now()`): not planned — minor; tests already cope via `isCloseTo`. Revisit if time-sensitive plan logic appears.
- **`RecipesResource` default-limit check**: out of scope of plan 002 but flagged there — the same absent-limit-means-unlimited pattern likely applies; audit it when touching recipes.
